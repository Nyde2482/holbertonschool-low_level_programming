#include "main.h"

/**
 * mul - multiply two integer
 * @a: first integer
 * @b: second integer
 * Return: the result
 */
int mul(int a, int b)
{
return (a * b);
}

int main(void)
{
	printf("%d", mul(3, 4))
}